#!/bin/bash

- name: variabels from files
  hosts: localhost
  user: centos
  become: yes
  connection: ssh
  vars:
  - variabels.yaml
  tasks:
  - name: testing variabels from files
    ansible.builtin.debug:
      mes: "i am taking {{Course}} from {{Trainer}}"