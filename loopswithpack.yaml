- name: installing packages
  hosts: localhost
  user: centos
  become: yes
  connection: ssh
  tasks:
  - name: installing git,mysql
    ansible.builtin.package:
     action: "{{item}}"
     state: present
    loop:
    - git
    - mysql 